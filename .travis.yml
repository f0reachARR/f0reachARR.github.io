language: node_js
node_js:
  - "node"
script: npm build
after_success:
  - rm -rf node_modules/
  - rm *.styl *.pug
  - rm .gitignore .travis.yml
deploy:
  provider: pages
  local-dir: build/
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master